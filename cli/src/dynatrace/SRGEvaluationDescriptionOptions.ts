export type SRGEvaluationDescriptionOptions = {
  extra_vars?: string[];
  buildId: string;
  releaseVersion: string;
  service: string;
  application: string;
  stage: string;
  provider: string;
};
